services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      DB_HOST: db
      DB_NAME: mydb
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: mydb